/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['img.freepik.com', 'via.placeholder.com','i.pinimg.com'],
    },
};

export default nextConfig;

